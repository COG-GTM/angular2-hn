/**
 * Settings interface for application preferences.
 * Based on the Angular app's settings.ts model.
 */
export interface Settings {
  showSettings: boolean;
  openLinkInNewTab: boolean;
  theme: string;
  titleFontSize: string;
  listSpacing: string;
}
