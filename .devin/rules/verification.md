# Verify changes against this checkout

- For Angular code or specs on the current Node 20/Angular 9 checkout, run tests without watch mode using `NODE_OPTIONS=--openssl-legacy-provider CHROME_BIN=$(find /opt/.devin/chrome -name chrome -type f | head -1) npm test -- --watch=false`. The legacy OpenSSL option is required by this checkout's Webpack 4 toolchain on Node 20; reassess it after a toolchain upgrade. The Chrome path is specific to the Devin environment.
- For code changes, run `npm run lint` and `npm run build` as applicable, and report their actual outcomes. If repository-wide lint fails in untouched source, verify changed TypeScript files separately with the existing TSLint configuration and distinguish baseline errors from new errors rather than silently treating lint as green.
- If adding a coverage gate, run its CI test command locally, confirm the test count and coverage output, and deliberately check that a threshold above the measured coverage exits nonzero before claiming the gate works. Revert the temporary threshold change.
