{
  "name": "react-hnpwa",
  "version": "0.0.0",
  "scripts": {
    "postinstall": "npm --prefix react-app install",
    "dev": "npm --prefix react-app run dev",
    "build": "npm --prefix react-app run build",
    "preview": "npm --prefix react-app run preview",
    "lint": "npm --prefix react-app run lint",
    "typecheck": "npm --prefix react-app run typecheck",
    "test": "npm --prefix react-app test"
  },
  "private": true,
  "prettier": {
    "trailingComma": "es5",
    "tabWidth": 4,
    "singleQuote": true,
    "printWidth": 120
  }
}
