language: node_js
node_js:
  - "20"

branches:
  only:
    - master

before_script:
  - npm install -g firebase-tools

install:
  - cd react-app && npm ci

script:
  - npm run lint
  - npm test
  - npm run build

after_success:
  - cd .. && firebase use default
  - firebase deploy --token $FIREBASE_TOKEN

notifications:
  email:
    on_failure: change
    on_success: change
