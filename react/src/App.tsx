import { Route, Routes } from 'react-router-dom';

function App() {
    return (
        <Routes>
            <Route index element={<div>Angular 2 HN — React</div>} />
        </Routes>
    );
}

export default App;
