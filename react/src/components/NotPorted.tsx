/**
 * Placeholder for an Angular component that has not been ported yet. Each
 * component PR listed in docs/MIGRATION.md replaces one of these usages.
 */
export function NotPorted({ name }: { name: string }) {
    return <div data-testid={`not-ported-${name}`} className="not-ported" />;
}
